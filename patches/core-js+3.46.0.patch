diff --git a/node_modules/core-js/internals/clamp.mjs b/node_modules/core-js/internals/clamp.mjs
new file mode 100644
index 0000000..53bbc29
--- /dev/null
+++ b/node_modules/core-js/internals/clamp.mjs
@@ -0,0 +1,10 @@
+// Added manually to satisfy Rollup import "./clamp.mjs" from globalThis-this.mjs
+// This stub prevents Rollup resolution errors. It exports a safe clamp function.
+
+export function clamp(value, min = 0, max = 1) {
+  if (value < min) return min;
+  if (value > max) return max;
+  return value;
+}
+
+export default clamp;
diff --git a/node_modules/core-js/internals/define-globalThis-property.js b/node_modules/core-js/internals/define-globalThis-property.js
new file mode 100644
index 0000000..7101f11
--- /dev/null
+++ b/node_modules/core-js/internals/define-globalThis-property.js
@@ -0,0 +1,3 @@
+// Added manually to fix Rollup import error in Vite builds
+import defineGlobalProperty from './define-global-property.js';
+export default defineGlobalProperty;
diff --git a/node_modules/core-js/internals/globalThis-config.mjs b/node_modules/core-js/internals/globalThis-config.mjs
new file mode 100644
index 0000000..ab0115a
--- /dev/null
+++ b/node_modules/core-js/internals/globalThis-config.mjs
@@ -0,0 +1,15 @@
+// Minimal stub to satisfy Rollup resolution
+// Do NOT re-export anything to avoid circular reference
+
+if (typeof globalThis === 'undefined') {
+  try {
+    // eslint-disable-next-line no-undef
+    window.globalThis = window;
+  } catch (_) {
+    // ignore in non-browser envs
+  }
+}
+
+// Provide a harmless placeholder export
+export const MotionGlobalConfig = {};
+export default MotionGlobalConfig;
diff --git a/node_modules/core-js/internals/globalThis-this.mjs b/node_modules/core-js/internals/globalThis-this.mjs
new file mode 100644
index 0000000..7cc0b46
--- /dev/null
+++ b/node_modules/core-js/internals/globalThis-this.mjs
@@ -0,0 +1,24 @@
+// Persisted patch: ensure globalThis exists and re-export real module API
+// (This file is created to satisfy Vite/Rollup ESM resolution issues)
+
+if (typeof globalThis === 'undefined') {
+  try {
+    // eslint-disable-next-line no-undef
+    window.globalThis = window;
+  } catch (e) {
+    // ignore in non-browser envs
+  }
+}
+
+// Re-export everything from the canonical module that motion-utils expects
+export * from './global-config.mjs';
+// export * from './clamp.mjs';
+// export * from './errors.mjs';
+// export * from './is-numerical-string.mjs';
+// export * from './is-object.mjs';
+// export * from './is-zero-value-string.mjs';
+// export * from './event.mjs';
+// export * from './value-presentation.mjs';
+
+// (You may optionally just re-export the specific symbol required:)
+export { MotionGlobalConfig } from './global-config.mjs';
